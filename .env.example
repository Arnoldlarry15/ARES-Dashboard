# Google Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
# The dashboard works without an API key using mock data

# For Vercel deployment, add this to your Vercel project environment variables:
# GEMINI_API_KEY=your_api_key_here
# (Note: no VITE_ prefix - this is for the backend API only)

# For local development with Vercel CLI (optional):
GEMINI_API_KEY=your_api_key_here
