{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ares-dashboard.fullname" . }}
  labels:
    {{- include "ares-dashboard.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "ares-dashboard.selectorLabels" . | nindent 6 }}
{{- end }}
